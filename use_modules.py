from modules import * # By specifying what file we import from and *, we import everything, therefore we don't have to specify the the file before using the function, e.g: modules.return_user_id

return_user_id()

# return_user_name()